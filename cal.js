function p(x) {
    console.log(x);
}

function add(x, y) {
    return x + y;
}

function sub(x, y) {
    return x - y;
}

function mul(x, y) {
    return x * y;
}

function div(x, y) {
    return x / y;
}

p(add(10,20));
p(sub(10,20));
p(mul(10,20));
p(div(10,20));

